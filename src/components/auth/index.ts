/**
 * GrowLab Auth Components Index
 *
 * Empty for now — auth-related UI lives inline in routes/login.tsx and
 * routes/register.tsx. Profile chrome (avatar dropdown, menu) lands in
 * F2/F6 per the Master Plan and will populate this barrel then.
 */

export {}
