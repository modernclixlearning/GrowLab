# ── Secretos y configuración local ───────────────────────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging
.env.production
# Permitir el archivo de ejemplo si el proyecto lo usa:
# !.env.example

# Claves, certificados y credenciales
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
service-account*.json

# ── Sistema operativo ─────────────────────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# ── Editores e IDEs ───────────────────────────────────────────────────────────
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# ── Dependencias (genérico) ───────────────────────────────────────────────────
node_modules/
vendor/
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# ── Builds y caches genéricos ─────────────────────────────────────────────────
dist/
build/
out/
.cache/
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Test coverage ─────────────────────────────────────────────────────────────
coverage/
.nyc_output/
htmlcov/

# ── Stack específico ──────────────────────────────────────────────────────────
# Agregar aquí las reglas específicas del stack del proyecto

# ── Repos Git anidados (working trees independientes con su propio remote) ────
# El wrapper no los rastrea como gitlinks; cada uno se gestiona desde su propio
# repo. Si en el futuro se decide convertirlos en submódulos formales, eliminar
# estas líneas y usar `git submodule add`.
GrowLab-agent1/
orchestrator/
